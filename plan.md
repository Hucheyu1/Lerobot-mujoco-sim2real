# UR5e 力矩控制与 IBKN 论文实验计划

## 1. 目标与约束

本分支从提交 `b34bba601887ee5424af942e65ac315972049873` 建立，目标不是重新设计一套与原论文无关的系统，而是把原 SOARM101 的数据流、Koopman 网络、分区损失和 MPC 逻辑迁移到支持直接关节力矩控制的 UR5e 上。

必须保持的核心逻辑：

1. 状态以前三维末端笛卡尔坐标开头。
2. DKUC、DBKN、IKN、IBKN 四个模型使用同一数据、划分、训练预算和评估协议。
3. 预测损失保留原来的“末端位置 + 4 倍关节角”分区权重。
4. MPC 直接读取学习到的 `A/B/H/C`，双线性模型在当前升维状态处构造 `B_total(z0)`。
5. 控制器输出为 6 维残差力矩，MuJoCo 执行器工作在直接 `motor` 力矩模式。

与 SOARM101 不同且必须增加的量是关节速度 `dq`。位置伺服状态 `[p_ee,q]` 对力矩动力学并非 Markov 状态；因此 UR5e 使用 `[p_ee,q,dq]`，而不是删除末端坐标或仅使用 `[q,dq]`。

## 2. 端到端流程、输入与输出

| 阶段 | 输入 | 处理 | 输出 |
|---|---|---|---|
| MuJoCo 环境 | 残差力矩 `u_t∈R^6`，N·m | `tau=clip(0.9*tau_g+u_t)`，以 0.002 s 物理步长积分 10 次 | `x_(t+1)=[p_ee,q,dq]∈R^15` |
| 数据采集 | 随机、正弦、chirp 力矩激励 | 保存严格对齐的转移 `(x_t,u_t,x_(t+1))` | 每行 `[u_t(6),p_ee(3),q(6),dq(6)]`，共 21 维 |
| DataLoader | 物理单位 `.npy` | 仅将力矩除以额定力矩；状态保持物理量 | 网络批次 `x:[B,T,15]`、`u:[B,T,6]` |
| 状态编码 | `x_t` | 普通 MLP 或可逆/注入式 iMLP | 升维状态 `z_t` |
| Koopman 动力学 | `z_t,u_t` | 线性或双线性状态转移 | `z_(t+1)` |
| 状态解码 | `z_(t+1)` | 固定读出或可逆解码 | `x_hat_(t+1)=[p_hat_ee,q_hat,dq_hat]` |
| MPC | 当前 `x_t`、未来参考 `[p_ref,q_ref,dq_ref]` | 升维、冻结双线性矩阵、有限时域优化 | 归一化控制及物理残差力矩 `u_t` |
| 闭环测试 | 参考轨迹、扰动、噪声配置 | MuJoCo + MPC/基线控制器 | EE RMSE、关节 RMSE、力矩、求解时间、安全终止率 |

数据时序定义为：第 `t` 行的 `u_t` 作用于同一行的 `x_t`，产生下一行的 `x_(t+1)`。训练和 MPC 都必须遵守该定义。

## 3. 四种网络的对照关系

| 模型 | 状态编码/解码 | 动力学 | 要验证的因素 |
|---|---|---|---|
| DKUC | 普通非线性编码 + 固定物理状态读出 | `z+=Az+Bu` | 基础 Koopman 对照 |
| DBKN | 与 DKUC 相同 | `z+=Az+Bu+H(z⊗u)` | 双线性项的收益 |
| IKN | 可逆/注入式 iMLP，解析逆映射 | `z+=Az+Bu` | 可逆结构的收益 |
| IBKN | 可逆/注入式 iMLP，解析逆映射 | `z+=Az+Bu+H(z⊗u)` | 可逆结构与双线性项的组合收益 |

双线性矩阵在 MPC 中按原项目方式展开：

`B_total(z0) = B + Σ_j z0[j] H_hat[j]`

并在一个 MPC 预测时域内固定 `B_total(z0)`。代码测试必须验证该矩阵展开与网络前向计算完全等价，且同时覆盖 `z⊗u`、`u⊗z` 两种排列。

## 4. 损失函数

从随机起点执行长度为 `K` 的开环递推，不能每一步用真实状态重新初始化升维状态。第 `k` 步物理预测损失为：

`L_phy(k) = L_ee(k) + 4 L_q(k) + L_dq(k)`

其中前两项严格延续 SOARM101 的 `L_ee + 4 L_q`，`L_dq` 是力矩动力学新增状态的误差。完整训练目标为：

`L_total = Σ gamma^k [L_phy(k) + L_koop(k)] / Σ gamma^k + L_recon + 1e-3 L_stable`

- `L_koop`：预测升维状态与真实下一状态编码结果之间的一致性。
- `L_recon`：仅对使用学习型非可逆解码器的模型启用；可逆模型不重复惩罚解析重构。
- `L_stable`：对非受约束 `A` 超出谱半径裕量 0.99 的部分施加软惩罚。
- `H` 的 L1 项继续记录为诊断量，默认权重为 `1e-9`，暂不主导训练。

报告必须分别给出 EE、关节角、关节速度和总体误差，不能只用一个总体 RMSE 掩盖量纲差异。

## 5. MPC 设计

1. 输入当前物理状态 `x_t∈R^15`，编码为 `z0`。
2. 普通编码模型通过固定 `C` 在物理空间计算跟踪代价；IKN/IBKN 将参考轨迹编码后在升维空间计算代价，与原控制器保持一致。
3. 标准 MPC 优化未来控制 `u`，控制惩罚为 `u'Ru`。
4. 增量 MPC 优化 `Δu`，递推 `u_k=u_(k-1)+Δu_k`，控制惩罚为 `Δu'RΔu`。
5. 物理状态权重从原来的 `[50×EE,1×q]` 扩展为 `[50×EE,1×q,1×dq]`；升维状态权重保持 `50I`，`R=0.5I`。
6. 约束同时包含残差力矩盒约束和相邻控制增量约束，优化变量使用按额定力矩归一化的量，最终输出恢复为 N·m。
7. 首选 CasADi/Ipopt；若当前 CasADi 安装缺少 Ipopt 动态库，则用 SciPy/SLSQP 求解同一目标和约束，并记录实际后端。

## 6. 已发现的代码与实验风险

| 风险 | 影响 | 修正/验证方法 |
|---|---|---|
| 曾删除末端坐标，只保留 `[q,dq]` | 偏离论文任务和原架构 | 已改为 `[p_ee,q,dq]`，增加顺序与维度测试 |
| 旧数据可能混用目标速度、控制命令和真实力矩 | 学到错误输入—状态关系 | 动作统一为实际施加前的残差力矩，保存物理单位 |
| 动作与状态错一拍 | 一步预测看似正常，多步和控制失效 | 明确定义 `u_t:x_t→x_(t+1)` 并做转移测试 |
| 训练/验证轨迹由同一随机序列衍生 | 数据泄漏、虚高泛化结果 | 使用独立 seed 和独立完整轨迹划分 |
| 样本少、轨迹类型单一 | 容易过拟合随机激励 | 增加 random/sin/chirp、初态、负载、外力和噪声覆盖 |
| EE 是 `q` 的确定函数，重复输入可能造成尺度偏置 | 网络过度依赖冗余特征 | 保留该输入以对齐论文，同时做有/无 EE 消融和特征标准化消融 |
| m、rad、rad/s 直接混合 | 某一量纲支配损失 | 分项报告；训练集统计量标准化作为后续对照，不用测试集统计量 |
| 只看一步误差 | 掩盖开环漂移 | 统一报告 1、5、10、25、50 步预测曲线 |
| 模型参数量不同 | IBKN 优势可能来自容量而非结构 | 增加等参数量 MLP/Koopman 对照并报告参数量 |
| 双线性 `H` 零初始化且训练不足 | DBKN/IBKN 退化为线性模型 | 记录 `||H||`、双线性贡献比和梯度范数 |
| 训练集低噪声、全状态反馈 | EKF/UKF 不可能优于直接测量 | 将滤波优势放在部分观测、传感噪声、丢包和模型扰动场景验证 |
| EKF/UKF 使用不一致的 Q/R 或观测模型 | 两者结果几乎相同或劣化 | 用创新白化、NIS/NEES 和网格/贝叶斯调参校准噪声协方差 |
| 在训练分布内调 MPC 权重 | 控制结果偏向某模型 | 用验证集统一选择 Q/R/H，测试集冻结所有超参数 |
| 简化 UR5e 模型未经实机辨识 | 不能直接声称 sim-to-real | 论文结论限定为 MuJoCo 仿真，后续再做域随机化/硬件验证 |

## 7. 实验矩阵

### 7.1 预测实验

- 正式默认数据规模与 SOARM101 对齐：训练 `50,000×20`，验证 `2,000×200`，每类测试 `2,000×200`。
- 控制/数据步长为 0.02 s；训练轨迹 0.4 s，验证/测试轨迹 4 s，多步训练窗口 10 步（0.2 s）。
- 四模型 × random/sin/chirp 三类未见轨迹。
- 5 个以上随机种子，报告均值、标准差和 95% 置信区间。
- 1/5/10/25/50 步 EE、q、dq RMSE/MAE。
- 参数量、训练耗时、推理耗时、`A` 谱半径、`H` 范数。

### 7.2 结构消融

- DKUC vs DBKN：只改变双线性项。
- IKN vs IBKN：只改变双线性项。
- DKUC vs IKN、DBKN vs IBKN：只改变可逆结构。
- IBKN 去掉 EE 输入、去掉 `dq`（仅作非 Markov 负对照）、关闭稳定性正则、不同 `H` 初始化。
- 等参数量和等训练预算对照。

### 7.3 控制实验

- PD、DKUC-MPC、DBKN-MPC、IKN-MPC、IBKN-MPC。
- 标准 MPC 与增量 MPC。
- 标称轨迹、未见频率轨迹、负载变化、外力脉冲、摩擦/质量随机化。
- 指标：EE/q RMSE、最大/均方力矩、控制增量、约束违反率、求解时间、安全终止率。

### 7.4 状态估计实验

- 直接测量、EKF、UKF 必须分别在低噪声全状态和高噪声/部分观测场景报告。
- 低噪声全状态下直接测量最好是合理结果，不能通过人为调参强迫 UKF 获胜。
- 新论文应把滤波贡献表述为“在不完美观测下提高鲁棒性”，而不是无条件优于真实全状态。

## 8. 执行顺序与验收标准

1. **架构对齐**：完成 15 维状态、21 维数据行、四模型维度和损失切片修改。
2. **MPC 对齐**：完成 `A/B/H/C` 读取、`B_total(z0)`、标准/增量 MPC 和约束。
3. **代码回归**：环境、数据、损失、双线性展开、四模型、PD、MPC 测试全部通过。
4. **冒烟闭环**：小数据完成采集、IBKN 训练、三类测试和至少 5 步 MPC；只验证链路，不作为论文结论。
5. **正式数据**：按训练 `50,000×20`、验证 `2,000×200`、每类测试 `2,000×200` 和固定 seed 清单重新采集，不复用冒烟数据。
6. **正式训练**：默认 500 epochs、batch size 256、10 步开环预测；所有模型使用相同早停、训练轮数上限和调参预算，保存 checkpoint、history、配置与环境版本。
7. **正式控制与估计**：完成全部扰动/噪声矩阵；失败或安全终止也必须记录。
8. **统计与论文**：由原始结果自动生成表格和图片；结论只接受统计支持，不以预设“IBKN 必须最好”篡改实验。

当前完成情况：步骤 1–4 已通过代码测试和小规模链路验证；步骤 5–8 需要在本架构冻结后重新执行，冒烟结果不得写入论文主结果表。
